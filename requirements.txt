# Core dependencies for image processing and data handling
Pillow>=12.0.0
numpy>=2.2.0,<2.3.0  # OpenCV and OpenVINO require <2.3.0
opencv-python>=4.12.0

# FastAPI server dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Process monitoring and naming
setproctitle>=1.3.0

# ONNX Runtime with OpenVINO support
onnxruntime-openvino>=1.16.0

# OpenVINO for inference optimization
openvino>=2023.0.0
onnx>=1.14.0
onnxscript>=0.1.0

# PyTorch packages with DirectML for AMD GPU (Windows)
# Install with: pip install torch-directml
# Note: Python 3.11 required (DirectML doesn't support 3.12+ yet)
torch==2.4.1
torchvision==0.19.1
torch-directml>=0.2.5

# Optional but recommended for better performance
# matplotlib>=3.5.0  # for visualization
# tqdm>=4.64.0       # for progress bars
# tensorboard>=2.9.0 # for logging and monitoring